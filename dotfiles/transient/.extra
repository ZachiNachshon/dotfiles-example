#!/bin/bash

########################
#       Shortcuts
########################
alias code="cd ${CODEBASE}"
alias github="cd ${CODEBASE}/github"
alias dl="cd ~/Downloads"
alias gop="cd ${GOPATH}/src/github.com"
alias dc="docker-compose"

# Exist only for Java 1.8 - use full path to use when consuming other Java versions
alias visualvm="/Library/Java/JavaVirtualMachines/jdk1.8.0_152.jdk/Contents/Home/bin/jvisualvm &"
#alias visual="/usr/bin/jvisualvm &"

# launchctl is MAC equivalent to systemctl
#alias service="launchctl"